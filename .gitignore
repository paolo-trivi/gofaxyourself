# === Secrets — NEVER commit ===
.env
.env.*
!.env.example

# === Local audio — only the bundled default ships. Drop your own WAVs in. ===
*.wav
!audio/it/anti-call-center.wav

# === Captured call logs — hold real numbers/identities. NEVER commit. ===
logs/
*.ndjson

# === macOS / editor noise ===
.DS_Store
